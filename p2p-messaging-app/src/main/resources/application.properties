# Session timeout configuration (optional, adjust as needed)
server.servlet.session.timeout=30m

# Enable session cookies (enabled by default, but explicitly set here)
server.servlet.session.cookie.enabled=true

# Enable persistent sessions (ensures the session isn't lost prematurely)
server.servlet.session.persistent=true